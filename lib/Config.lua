--[=[
    @class Config

    This is the [LeaderboardExtended] configuration and allows you to configure
    customized behavior for the Leaderboard.

    ```lua
        local Config = {
            DefaultIcon = "rbxassetid://0000000"
        };
    ```
]=]

--[=[
    @prop DefaultIcon string
    @within Config

    This config is for changing the default player icon and it should represent a string which correlates to an image id.
]=]
return true;